# Third-Party Source

Estimator and sensor-driver repositories selected for this project are pinned
under this directory. Do not import code from sibling workspace folders.

Each dependency must record its upstream URL, revision, license, local patches,
and supported Jetson/ROS version before it enters the flight runtime.
